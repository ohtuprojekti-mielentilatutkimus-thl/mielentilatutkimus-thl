{
  "name": "mielentila-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand",
    "lint": "eslint .",
    "dev": "NODE_ENV=dev node index.js",
    "prod": "NODE_ENV=production node index.js",
    "build:thl": "rm -rf build && cd ../thl-frontend && npm run build --prod && cp -r build ../backend",
    "build:ui": "rm -rf build && cd ../frontend/ && npm run build --prod && cp -r build ../backend",
    "start:cypress": "cross-env NODE_ENV=test node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "cross-env": "^7.0.3",
    "express": "^4.17.1",
    "maildev": "^1.1.0",
    "mongoose": "^6.0.5",
    "morgan": "^1.10.0",
    "multer": "^1.4.3",
    "nodemailer": "^6.6.3"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^27.2.2",
    "supertest": "^6.1.6"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "<rootDir>/utils/",
      "<rootDir>/node_modules"
    ]
  }
}
